name: Deploy to AWS Lambda

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0'

    - name: Install dependencies
      run: dotnet restore TesteLambda/TesteLambda/TesteLambda.csproj

    - name: Build project
      run: dotnet build TesteLambda/TesteLambda/TesteLambda.csproj --configuration Release
    
    - name: Build tests
      run: dotnet build TesteLambda/TesteLambda.Tests/TesteLambda.Tests.csproj --configuration Release

    - name: Run tests
      run: dotnet test TesteLambda/TesteLambda.Tests/TesteLambda.Tests.csproj --configuration Release

    - name: Install Lambda Tools
      run: dotnet tool install -g Amazon.Lambda.Tools

    - name: Add Lambda Tools to PATH
      shell: bash
      run: echo "$HOME/.dotnet/tools" >> $GITHUB_PATH

    - name: Package Lambda
      run: |
        cd TesteLambda/TesteLambda
        dotnet lambda package --configuration Release --output-package ../../package.zip

    - name: Deploy to AWS Lambda
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_REGION: 'sa-east-1'
      run: |
        aws lambda update-function-code --function-name executarRpaBoleto --zip-file fileb://package.zip
